<!--pages/book/index.wxml-->
<yc-tab 
    tabs="{{ tabs }}" 
    currentIndex="{{ tabActiveIndex }}"
    bindchange="handleTabChange"
/>
<swiper class="swiper {{ isLogin ? 'login' : '' }}" duration="{{ 300 }}" current="{{ tabActiveIndex }}" bindchange="handleSwiperChange">
    <swiper-item>
        <block wx:if="{{ !isLogin }}">
            <yc-center-image />
        </block>
        <block wx:elif="{{ alumniBooks.length === 0 }}">
            <yc-center-image />
            <view class="create-btn" bindtap="handleCreateBook"></view>
        </block>
        <block wx:else>
            <scroll-view style="height: 100%;" scroll-y>
                <yc-alumni wx:for="{{ 5 }}" wx:key="*this"/>
            </scroll-view>
            <view class="create-btn" bindtap="handleCreateBook"></view>
        </block>
    </swiper-item>
    <swiper-item>
        <block wx:if="{{ !isLogin }}">
            <yc-center-image />
        </block>
        <block wx:elif="{{ joins.length === 0 }}">
            <image class="image" mode="aspectFit" src="/images/message.png"/>
            asd
        </block>
        <block wx:else>
            <yc-user-list/>
        </block>
    </swiper-item>
</swiper>
<block wx:if="{{ !isLogin }}">
    <yc-login-bar/>
</block>
